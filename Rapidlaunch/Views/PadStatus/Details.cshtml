@model Rapidlaunch.Models.PadStatus

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>PadStatus</h4>
    <hr />
    <dl class="row" style="text-align:center">
        <!--Align more Central  -->
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.padStatus)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.padStatus)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.PadStatusID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
