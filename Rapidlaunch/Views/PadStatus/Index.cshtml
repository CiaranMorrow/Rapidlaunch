@model IEnumerable<Rapidlaunch.Models.PadStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.padStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.padStatus)
            </td>
            <td class="tester-editor">
                <div class="dropdownIndex">
                    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
                        Data Option
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu">
                        <a asp-action="Edit" asp-route-id="@item.PadStatusID">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.PadStatusID">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.PadStatusID">Delete</a>
                    </ul>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
