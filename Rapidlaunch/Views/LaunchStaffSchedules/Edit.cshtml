@model Rapidlaunch.Models.LaunchStaffSchedule

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>LaunchStaffSchedule</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="LaunchStaffScheduleID" />
            <div class="form-group">
                <label asp-for="launchID" class="control-label"></label>
                <select asp-for="launchID" class="form-control" asp-items="ViewBag.launchID"></select>
                <span asp-validation-for="launchID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="staffID" class="control-label"></label>
                <select asp-for="staffID" class="form-control" asp-items="ViewBag.staffID"></select>
                <span asp-validation-for="staffID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
