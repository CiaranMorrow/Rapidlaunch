@model Rapidlaunch.Models.StaffSafetyRecord

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>StaffSafetyRecord</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="staffID" />
            <input type="hidden" asp-for="safetyRatingID" />
            <div class="form-group">
                <label asp-for="rocketTypeID" class="control-label"></label>
                <select asp-for="rocketTypeID" class="form-control" asp-items="ViewBag.rocketTypeID"></select>
                <span asp-validation-for="rocketTypeID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
