@model Rapidlaunch.Models.StaffSafetyRecord

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>StaffSafetyRecord</h4>
<hr />
<div class="row justify-content-md-center"> <!-- Centering the Row -->>
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="staffID" class="control-label"></label>
                <select asp-for="staffID" class ="form-control" asp-items="ViewBag.staffID"></select>
            </div>
            <div class="form-group">
                <label asp-for="safetyRatingID" class="control-label"></label>
                <select asp-for="safetyRatingID" class ="form-control" asp-items="ViewBag.safetyRatingID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div class="backtolist">
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
